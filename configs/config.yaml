project:
  name: vietedusent
  seed: 42

data:
  path: data/processed/processed.csv
  test_size: 0.2

model:
  type: svm  # svm | logistic | rf | gru | phobert

training:
  batch_size: 32
  epochs: 5
  learning_rate: 2e-5

experiment:
  kfold: 5
  tracking: mlflow
